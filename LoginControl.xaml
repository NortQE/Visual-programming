<UserControl x:Class="LoginApp.LoginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LoginApp"
             Height="Auto" Width="Auto">

    <!-- Стили -->
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FF4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="16" Width="250">
        <TextBlock Text="Ім'я користувача:" Margin="0,0,0,4" />
        <TextBox x:Name="UsernameBox" />

        <TextBlock Text="Пароль:" Margin="0,8,0,4" />
        <PasswordBox x:Name="PasswordBox">
            <PasswordBox.InputBindings>
                <KeyBinding Key="Enter" Command="{x:Static local:LoginControl.LoginCommand}" />
            </PasswordBox.InputBindings>
        </PasswordBox>

        <Button Style="{StaticResource PrimaryButton}"
                x:Name="LoginButton"
                Content="Увійти"
                Margin="0,12,0,0"
                Height="30"
                Command="{x:Static local:LoginControl.LoginCommand}" />

        <TextBlock x:Name="MessageText"
                   Margin="0,12,0,0"
                   TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
